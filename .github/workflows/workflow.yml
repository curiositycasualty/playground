name: Build & Test
on:
  push:
    paths-ignore: &ignore-md
      - '**.md'
  pull_request:
    <<: *ignore-md

jobs:
  build:
    name: Build dependencies
    runs-on: ubuntu-22.04

    steps:
      - name: Run the script
        run: uptime
